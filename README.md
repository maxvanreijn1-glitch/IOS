# Meetings Managed

A **100% native** meeting scheduling and management app built with **Expo + React Native** (iOS & Android) and **Next.js** (web).

---

## Mobile App — Screens

| Screen | Description |
|---|---|
| **Login** | Email/password form with basic validation. Any email + 6+ char password signs you in. |
| **Meetings List** | `FlatList` of all meetings with status indicators (upcoming / completed / cancelled). |
| **Meeting Details** | Full meeting info — date, duration, organiser, location, description — plus Accept/Decline actions for upcoming meetings. |
| **Settings** | Signed-in user info, app version, and Sign Out button. |

### Architecture

```
src/
├── context/
│   └── AuthContext.tsx         # Auth state (sign in / sign out)
├── data/
│   └── meetings.ts            # In-memory mock meetings + helper fns
├── navigation/
│   ├── types.ts               # TypeScript param lists for all stacks
│   ├── AuthStack.tsx          # Unauthenticated stack (Login)
│   ├── AppTabs.tsx            # Authenticated tabs (Meetings + Settings)
│   └── RootNavigator.tsx      # Switches between Auth/App based on auth state
└── screens/
    ├── LoginScreen.tsx
    ├── MeetingsListScreen.tsx
    ├── MeetingDetailsScreen.tsx
    └── SettingsScreen.tsx
```

---

## Repository Structure

```
.
├── App.tsx                 # Expo/React Native entry point
├── app.config.ts           # Expo configuration
├── babel.config.js         # Babel config for Expo
├── src/                    # Native app source (see above)
├── assets/                 # App icons, splash screens
├── web/                    # Next.js web application
│   ├── app/                # Next.js App Router pages
│   ├── components/         # Shared React components (web)
│   ├── context/            # React context providers (web)
│   ├── lib/                # Server utilities, Prisma, Stripe
│   ├── prisma/             # Database schema & migrations
│   ├── public/             # Static assets for web
│   ├── next.config.ts
│   ├── tsconfig.json
│   └── package.json
├── package.json            # Root: Expo/React Native deps & scripts
└── tsconfig.json           # Root: Expo/React Native TypeScript config
```

---

## Getting Started — iOS / React Native

### Prerequisites

- [Node.js](https://nodejs.org/) (v18+)
- [Expo CLI](https://docs.expo.dev/more/expo-cli/): `npm install -g expo-cli`
- [Xcode](https://developer.apple.com/xcode/) (for iOS, macOS only)
- iOS Simulator or physical device with [Expo Go](https://expo.dev/client)

### Install dependencies

```bash
npm install
```

### Run in Expo Go (development)

```bash
npm run start        # Start Metro bundler
npm run ios          # Open iOS simulator
npm run android      # Open Android emulator
```

> **Sign in:** Enter any valid email address and a password of 6+ characters on the Login screen.

### Generate native iOS project (Xcode)

To generate the `ios/` directory and open the project in Xcode:

```bash
npm run prebuild          # or: npx expo prebuild
# Opens Xcode workspace:
open ios/MeetingsManaged.xcworkspace
```

> **Note:** The `ios/` and `android/` directories are generated by `expo prebuild` and are excluded from version control (`.gitignore`). Re-run `expo prebuild` to regenerate them after a fresh clone.

### Build for iOS in Xcode

1. Run `npm run prebuild` to generate `ios/`
2. Open `ios/MeetingsManaged.xcworkspace` in Xcode
3. Select your target device/simulator
4. Press **Cmd+R** or click **Run**

---

## Getting Started — Web (Next.js)

The web app lives in the `web/` subdirectory.

```bash
cd web
npm install
```

### Development server

```bash
npm run web:dev       # from root, or:
cd web && npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

### Environment Variables

Copy `web/.env.example` to `web/.env.local` and fill in your values:

```bash
cp web/.env.example web/.env.local
```

| Variable | Description |
|---|---|
| `DATABASE_URL` | PostgreSQL pooled connection URL |
| `DIRECT_DATABASE_URL` | PostgreSQL direct connection URL (for Prisma Migrate) |
| `RESEND_API_KEY` | Resend API key for transactional emails |
| `EMAIL_FROM` | Verified sender email address |
| `NEXT_PUBLIC_URL` | Public base URL of your deployment |

### Prisma / Database

```bash
cd web
npx prisma migrate dev   # Run migrations (uses DIRECT_DATABASE_URL)
npx prisma generate      # Regenerate Prisma client
```

---

## Scripts (root)

| Script | Description |
|---|---|
| `npm run start` | Start Expo Metro bundler |
| `npm run ios` | Run on iOS simulator |
| `npm run android` | Run on Android emulator |
| `npm run web` | Run Expo web target |
| `npm run prebuild` | Generate `ios/` and `android/` native projects |
| `npm run prebuild:ios` | Generate `ios/` native project only |
| `npm run web:dev` | Start Next.js dev server |
| `npm run web:build` | Build Next.js for production |
| `npm run web:start` | Start Next.js production server |

---

## VS Code

Open the root folder in VS Code. The project is configured for TypeScript with Expo's base tsconfig.

Recommended extensions:
- [Expo Tools](https://marketplace.visualstudio.com/items?itemName=expo.vscode-expo-tools)
- [React Native Tools](https://marketplace.visualstudio.com/items?itemName=msjsdiag.vscode-react-native)
- [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint)
- [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)
